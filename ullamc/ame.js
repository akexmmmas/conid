const coordinates = [LONGITUDE2, latitude];
const suffix = coordinates[0] === LONGITUDE2 ? '2' : '';

// Now suffix will be '2' if coordinates[0] is LONGITUDE2, otherwise it will be an empty string.
